name: Build CachyLLama + ROCm

on:
  workflow_dispatch:
    inputs:
      gfx_target:
        description: 'AMD GPU targets (comma-separated)'
        required: false
        default: 'gfx1151,gfx1150,gfx120X,gfx110X,gfx103X,gfx90a,gfx908'
      backend:
        description: 'Backend: rocm, vulkan, or both'
        required: false
        default: 'both'
      cachyllama_ref:
        description: 'CachyLLama ref (tag/branch/commit) or "master"'
        required: false
        default: 'master'
      rocm_version:
        description: 'TheRock ROCm version or "latest"'
        required: false
        default: 'latest'
      create_release:
        description: 'Create a GitHub release after successful build'
        required: false
        default: true
        type: boolean

  push:
    tags:
      - 'v*'

env:
  GFX_TARGETS: ${{ github.event.inputs.gfx_target || 'gfx1151,gfx1150,gfx120X,gfx110X,gfx103X,gfx90a,gfx908' }}
  BACKEND: ${{ github.event.inputs.backend || 'both' }}
  CACHYLLAMA_REF: ${{ github.event.inputs.cachyllama_ref || 'master' }}
  ROCM_VERSION: ${{ github.event.inputs.rocm_version || 'latest' }}

jobs:
  prepare-matrix:
    runs-on: ubuntu-24.04
    outputs:
      ubuntu_matrix: ${{ steps.set-matrix.outputs.ubuntu_matrix }}
    steps:
      - id: set-matrix
        run: |
          targets="${{ env.GFX_TARGETS }}"
          backend="${{ env.BACKEND }}"
          # gfx90a / gfx908 are CDNA — ROCm only, skip Vulkan.
          cdna="gfx90a gfx908"
          rows='[]'
          for gfx in $(echo "$targets" | tr ',' ' '); do
            for b in rocm vulkan; do
              if [ "$backend" != "both" ] && [ "$backend" != "$b" ]; then continue; fi
              if echo "$cdna" | grep -qw "$gfx" && [ "$b" = "vulkan" ]; then continue; fi
              rows=$(echo "$rows" | jq -c --arg g "$gfx" --arg b "$b" '. + [{gfx_target:$g, backend:$b}]')
            done
          done
          ubuntu_matrix=$(echo "$rows" | jq -c '{include: .}')
          echo "ubuntu_matrix=$ubuntu_matrix" >> "$GITHUB_OUTPUT"
          echo "Matrix: $ubuntu_matrix"

  build-ubuntu:
    runs-on: ubuntu-24.04
    needs: prepare-matrix
    strategy:
      matrix: ${{ fromJson(needs.prepare-matrix.outputs.ubuntu_matrix) }}
      fail-fast: false
    outputs:
      rocm_version: ${{ steps.set-outputs.outputs.rocm_version }}
      cachyllama_commit: ${{ steps.set-outputs.outputs.cachyllama_commit }}
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Free disk space
        run: curl -fsSL https://raw.githubusercontent.com/kou/arrow/e49d8ae15583ceff03237571569099a6ad62be32/ci/scripts/util_free_space.sh | bash

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build unzip curl zstd patchelf jq libvulkan-dev glslc spirv-headers

      - name: Download and extract ROCm directly to /opt/rocm
        run: |
          # Determine ROCm version to use
          rocm_version="${{ env.ROCM_VERSION }}"
          current_target="${{ matrix.gfx_target }}"
          
          # Add appropriate suffixes for different GPU targets
          s3_target="$current_target"
          if [[ "$current_target" = "gfx103X" || "$current_target" = "gfx110X" || "$current_target" = "gfx120X" ]]; then
            s3_target="${current_target}-all"
            echo "Using target with -all suffix: $s3_target"
          fi
          
          # TheRock now publishes nightlies to the multi-arch tarball index. That
          # index is a static HTML page with an embedded JSON `files` array (name +
          # mtime), so we parse that instead of the retired S3 XML bucket listing.
          base_url="https://rocm.nightlies.amd.com/tarball-multi-arch"
          if [ "$rocm_version" = "latest" ]; then
            echo "Auto-detecting latest ROCm version for target: $current_target"
            prefix="therock-dist-linux-${s3_target}-"
  
            files_json=$(curl -s "$base_url/" | tr '\n' ' ' | grep -oP 'const files = \K\[.*?\](?=\s*;)')
            if [ -z "$files_json" ]; then
              echo "Failed to parse file index from $base_url/"
              exit 1
            fi
  
            # Pick the newest by the YYYYMMDD build date embedded in the filename.
            # Anchor the match so the version comes immediately after the prefix.
            # This excludes variants like "...-all-tests-<version>.tar.gz" that
            # TheRock also publishes and share the same prefix/date.
            latest_file=$(echo "$files_json" | jq -r --arg p "$prefix" \
              '[.[] | select(.name | test("^" + $p + "[0-9]+\\.[0-9]+\\.[0-9]+(a|rc)[0-9]+\\.tar\\.gz$"))] | sort_by(.name | capture("(?<d>[0-9]{8})\\.tar\\.gz$").d) | last | .name // empty')
            if [ -z "$latest_file" ]; then
              echo "No tarball found for prefix '$prefix' at $base_url/"
              exit 1
            fi
            echo "Found latest file: $latest_file"
  
            # Extract version from the filename for environment variable
            if [[ "$latest_file" =~ therock-dist-linux-${s3_target}-([0-9]+\.[0-9]+\.[0-9]+(a|rc)[0-9]+)\.tar\.gz ]]; then
              rocm_version="${BASH_REMATCH[1]}"
              echo "Detected latest ROCm version: $rocm_version"
            else
              echo "Failed to extract ROCm version from latest file: $latest_file"
              echo "Expected pattern: therock-dist-linux-${s3_target}-<version>.tar.gz"
              exit 1
            fi
  
            rocm_url="$base_url/$latest_file"
          else
            # For specific versions, construct the URL directly
            rocm_url="$base_url/therock-dist-linux-${s3_target}-${rocm_version}.tar.gz"
          fi
          
          # Store the version for use in other steps
          echo "DETECTED_ROCM_VERSION=$rocm_version" >> $GITHUB_ENV
          
          echo "Streaming ROCm from: $rocm_url directly to extraction"
          
          # Create directory if it doesn't exist
          sudo mkdir -p /opt/rocm
          
          # Stream download directly into tar extraction (no intermediate file)
          curl -sL "$rocm_url" | sudo tar --use-compress-program=gzip -xf - -C /opt/rocm --strip-components=1

      - name: Set ROCm environment variables
        run: |
          echo "Setting ROCm environment variables..."
          
          # Set environment variables for this step and subsequent steps
          echo "HIP_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "ROCM_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "HIP_PLATFORM=amd" >> $GITHUB_ENV
          echo "HIP_CLANG_PATH=/opt/rocm/llvm/bin" >> $GITHUB_ENV
          echo "HIP_INCLUDE_PATH=/opt/rocm/include" >> $GITHUB_ENV
          echo "HIP_LIB_PATH=/opt/rocm/lib" >> $GITHUB_ENV
          echo "HIP_DEVICE_LIB_PATH=/opt/rocm/lib/llvm/amdgcn/bitcode" >> $GITHUB_ENV
          
          # Update PATH
          echo "/opt/rocm/bin:/opt/rocm/llvm/bin:$PATH" >> $GITHUB_PATH
          
          # Set library paths
          echo "LD_LIBRARY_PATH=/opt/rocm/lib:/opt/rocm/lib64:/opt/rocm/llvm/lib:${LD_LIBRARY_PATH:-}" >> $GITHUB_ENV
          echo "LIBRARY_PATH=/opt/rocm/lib:/opt/rocm/lib64:${LIBRARY_PATH:-}" >> $GITHUB_ENV
          echo "CPATH=/opt/rocm/include:${CPATH:-}" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=/opt/rocm/lib/pkgconfig:${PKG_CONFIG_PATH:-}" >> $GITHUB_ENV
          
          echo "ROCm environment variables set successfully"

      - name: Clone CachyLLama
        run: |
          ref="${{ env.CACHYLLAMA_REF }}"
          git clone --no-checkout --filter=tree:0 https://github.com/fewtarius/CachyLLama.git llama.cpp
          cd llama.cpp
          # Resolve ref to a commit. Works for SHA, tag, or branch.
          # `git fetch --depth 1 origin <ref>` works for tags/branches; for a raw
          # SHA on github.com it also succeeds (GitHub serves SHAs directly).
          git fetch --depth 1 origin "$ref"
          git checkout FETCH_HEAD
          echo "CACHYLLAMA_COMMIT=$(git rev-parse --short=5 HEAD)" >> "$GITHUB_ENV"
          git log --oneline -1

      - name: Build (ROCm)
        if: matrix.backend == 'rocm'
        env:
          GFX_TARGET: ${{ matrix.gfx_target }}
          ROCM_PATH: /opt/rocm
          LLAMA_DIR: ./llama.cpp
        run: ./scripts/build-rocm.sh

      - name: Build (Vulkan)
        if: matrix.backend == 'vulkan'
        env:
          LLAMA_DIR: ./llama.cpp
        run: ./scripts/build-vulkan.sh

      - name: Bundle ROCm libs
        if: matrix.backend == 'rocm'
        env:
          ROCM_PATH: /opt/rocm
          BUILD_BIN: ./llama.cpp/build/bin
        run: ./scripts/bundle-rocm-libs.sh

      - name: Smoke check — binary runs + links
        if: always()
        run: |
          cd llama.cpp/build/bin
          if [ -x llama-server ]; then
            ./llama-server --version
            if ldd llama-server 2>&1 | grep -q "not found"; then
              echo "ldd reports missing libs"; exit 1
            fi
          else
            echo "llama-server not found"; exit 1
          fi

      - name: Determine release tag
        id: tag
        run: |
          if [ "${GITHUB_REF_TYPE:-}" = "tag" ]; then
            echo "tag=${GITHUB_REF##*/}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=nightly-pending" >> "$GITHUB_OUTPUT"
          fi

      - name: Make archive
        env:
          TAG: ${{ steps.tag.outputs.tag }}
          BACKEND: ${{ matrix.backend }}
          GFX_TARGET: ${{ matrix.gfx_target }}
          BUILD_BIN: ./llama.cpp/build/bin
        run: ./scripts/make-archive.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cachy-ubuntu-${{ matrix.backend }}-${{ matrix.gfx_target }}-x64
          path: cachy-*-ubuntu-${{ matrix.backend }}-${{ matrix.gfx_target }}-x64.tar.zst
          retention-days: 30

      - id: set-outputs
        if: always()
        run: |
          echo "rocm_version=${DETECTED_ROCM_VERSION:-none}" >> "$GITHUB_OUTPUT"
          echo "cachyllama_commit=${CACHYLLAMA_COMMIT:-none}" >> "$GITHUB_OUTPUT"

  create-release:
    needs: [prepare-matrix, build-ubuntu]
    runs-on: ubuntu-24.04
    if: always() && needs.build-ubuntu.result == 'success' && (github.event_name != 'workflow_dispatch' || github.event.inputs.create_release != 'false')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./all-artifacts

      - name: Resolve release tag
        id: rel
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${GITHUB_REF_TYPE:-}" = "tag" ]; then
            TAG="${GITHUB_REF##*/}"
          else
            existing=$(gh release list --limit 1000 --json tagName --jq '.[].tagName' | grep -E '^b[0-9]{4}$' | sort -V || true)
            if [ -z "$existing" ]; then n=1000; else n=$(( $(echo "$existing" | tail -n1 | sed 's/^b//') + 1 )); fi
            TAG=$(printf "b%04d" "$n")
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "prerelease=$([ "${GITHUB_REF_TYPE:-}" = "tag" ] && echo false || echo true)" >> "$GITHUB_OUTPUT"

      - name: Rename artifacts with tag
        run: |
          TAG="${{ steps.rel.outputs.tag }}"
          mkdir -p out
          for d in all-artifacts/cachy-ubuntu-*; do
            [ -d "$d" ] || continue
            base=$(basename "$d")              # cachy-ubuntu-rocm-gfx1151-x64
            backend=$(echo "$base" | cut -d- -f3)
            gfx=$(echo "$base" | cut -d- -f4)
            mv "$d"/*.tar.zst "out/cachy-${TAG}-ubuntu-${backend}-${gfx}-x64.tar.zst"
          done
          ls -la out

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.rel.outputs.tag }}"
          PRERELEASE="${{ steps.rel.outputs.prerelease }}"
          args=()
          [ "$PRERELEASE" = "true" ] && args+=(--prerelease)
          [ "$PRERELEASE" = "false" ] && args+=(--latest)
          gh release create "$TAG" out/*.tar.zst --title "$TAG" --notes "$(cat <<EOF
          **Build**: $TAG
          **gfx**: ${{ env.GFX_TARGETS }}
          **Backend**: ${{ env.BACKEND }}
          **ROCm**: ${{ needs.build-ubuntu.outputs.rocm_version }}
          **CachyLLama**: ${{ needs.build-ubuntu.outputs.cachyllama_commit }}
          **Built**: $(date -u '+%Y-%m-%d %H:%M UTC')
          EOF
          )" "${args[@]}"
